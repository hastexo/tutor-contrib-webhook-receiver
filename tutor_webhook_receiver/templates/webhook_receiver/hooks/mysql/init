mysql -u {{ MYSQL_ROOT_USERNAME }} --password="{{ MYSQL_ROOT_PASSWORD }}" --host "{{ MYSQL_HOST }}" --port {{ MYSQL_PORT }} -e 'CREATE DATABASE IF NOT EXISTS {{ WEBHOOK_RECEIVER_DB_NAME }};'
mysql -u {{ MYSQL_ROOT_USERNAME }} --password="{{ MYSQL_ROOT_PASSWORD }}" --host "{{ MYSQL_HOST }}" --port {{ MYSQL_PORT }} -e 'GRANT ALL ON {{ WEBHOOK_RECEIVER_DB_NAME }}.* TO "{{ WEBHOOK_RECEIVER_DB_USER }}"@"%" IDENTIFIED BY "{{ WEBHOOK_RECEIVER_DB_PASSWORD }}";'
